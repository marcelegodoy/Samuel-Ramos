<%= simple_form_for [:admin, @text] do |f| %>
  <%= f.input :identifier, label:"Identificador" %>
  <%= f.input :content, label:"Conteúdo" %>
  <div class="form-actions">
    <%= f.button :submit, "Salvar", class:"button-black" %>
  </div>
<% end %>
