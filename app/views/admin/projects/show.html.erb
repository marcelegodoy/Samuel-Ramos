<div class="dashboard-header">
  <h1><%= @project.title %></h1>
  <%= link_to "< voltar", admin_projects_path, class:"button-black" %>
</div>

<p><%= @project.description  %></p>
<% if @project.image.present? %>
  <%= image_tag @project.image %>
<% else %>
  <p>Nenhuma imagem anexada</p>
<% end %>
